apiVersion: certificates.k8s.io/v1
kind: CertificateSigningRequest
metadata:
  name: vc-webhook.default
spec:
  request: PLACEHOLDER_CSR_BASE64
  signerName: kubernetes.io/kubelet-serving
  usages:
  - digital signature
  - key encipherment
  - server auth
